"""
提示模板
用于AI提取目录的提示模板
"""

# 从文档中提取需求目录的提示模板
CATALOG_EXTRACTION_PROMPT = """
你是一位需求规格说明文档专家，我需要你帮我从以下文档中提取所有功能需求的标题和章节号。

请仔细阅读文档内容，然后按照以下规则提取：
1. 主要关注第3章（功能章节），提取所有与功能需求相关的标题和章节号
2. 章节号格式应为"3.x.x"或"3.x.x.x"或"3.x.x.x.x"等
3. 每个需求条目应包含章节号和标题
4. 只提取最小粒度的需求条目，不包括父章节
5. 需求级别设置为{requirement_level}（3级表示提取如"3.2.1 xxx"的条目，4级表示提取如"3.2.1.1 xxx"的条目，5级表示提取如"3.2.1.1.1 xxx"的条目）

文档内容如下：
```
{document_content}
```

请提取并列出所有符合条件的需求，以JSON格式返回结果，格式如下：
```json
{{
  "requirements": [
    {{
      "chapter": "3.1.1",
      "name": "3.1.1 需求名称1"
    }},
    {{
      "chapter": "3.2.3.1",
      "name": "3.2.3.1 需求名称2"
    }}
  ]
}}
```

注意：
1. 仅返回符合指定需求级别的条目
2. 确保提取的是真正的需求条目，而不是章节标题
3. 如果找不到任何符合条件的需求，返回空数组
4. 只返回JSON格式数据，不要有其他说明文字
"""

# 连续处理大文档的提示模板
CATALOG_CONTINUATION_PROMPT = """
你是一位需求规格说明文档专家，我需要你帮我从以下文档中提取所有功能需求的标题和章节号。这是文档的第{chunk_number}部分，请继续前面的提取工作。

请仔细阅读文档内容，然后按照以下规则提取：
1. 主要关注第3章（功能章节），提取所有与功能需求相关的标题和章节号
2. 章节号格式应为"3.x.x"或"3.x.x.x"或"3.x.x.x.x"等
3. 每个需求条目应包含章节号和标题
4. 只提取最小粒度的需求条目，不包括父章节
5. 需求级别设置为{requirement_level}（3级表示提取如"3.2.1 xxx"的条目，4级表示提取如"3.2.1.1 xxx"的条目，5级表示提取如"3.2.1.1.1 xxx"的条目）

文档内容如下：
```
{document_content}
```

请提取并列出所有符合条件的需求，以JSON格式返回结果，格式如下：
```json
{{
  "requirements": [
    {{
      "chapter": "3.1.1",
      "name": "3.1.1 需求名称1"
    }},
    {{
      "chapter": "3.2.3.1",
      "name": "3.2.3.1 需求名称2"
    }}
  ]
}}
```

注意：
1. 仅返回符合指定需求级别的条目
2. 确保提取的是真正的需求条目，而不是章节标题
3. 如果找不到任何符合条件的需求，返回空数组
4. 只返回JSON格式数据，不要有其他说明文字
5. 不要重复已提取的内容，只返回新发现的需求
"""
